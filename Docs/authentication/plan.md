You are a expert in building authentication for security 
Now you are given a task to implement a authentication in a application 
the technologies used are Python,Fastapi and postgrasql
Now when ever the user comes 2 options
1.Login 
2.Signup
so if the user logins he the auth must verify the email and password search for rmail and then verify the passowrd if both are correct then generate the token in the backend and then share it to the user who logged in
the token has certian time then it experies so if it is close to expire then generate the new one sharing of that
also focus on rate limiting 

2.this is creating a new account 
the 